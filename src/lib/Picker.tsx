import { FunctionComponent } from "preact"

type Props = {
  value: string
  onChange: (newValue: string) => void
  nrRows: number
}

export const Picker: FunctionComponent<Props> = ({
  value, onChange, nrRows
}) => {
  return <div>
  </div>

}
