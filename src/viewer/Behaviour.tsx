import { FunctionComponent } from "preact"
import * as css from "./viewer.module.css"

export const Behaviour: FunctionComponent = () => {
  return <div className={css.behaviour}>
    Behaviour
  </div>
}
